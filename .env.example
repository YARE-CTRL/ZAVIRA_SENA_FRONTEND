# ⚠️ ESTE ES UN ARCHIVO DE EJEMPLO - NO CONTIENE VALORES REALES
# Copia este archivo a .env y completa con tus valores reales

# API Backend URL
# RAMA NGROK-BACKUP: usa URL completa de ngrok
# En producción (Vercel): usa /api (se redirige via vercel.json al backend ngrok)
# En desarrollo local: usa /api (se redirige via vite proxy)
VITE_API_URL=/api

# Entorno
VITE_ENV=production

# NOTAS RAMA NGROK-BACKUP:
# - Esta rama usa ngrok: https://gillian-semiluminous-blubberingly.ngrok-free.dev
# - NO subas archivos .env a Git
# - Cada desarrollador debe tener su propio .env
# - En producción (Vercel), configura VITE_API_URL=/api en Environment Variables
# - El vercel.json redirige /api/* a https://gillian-semiluminous-blubberingly.ngrok-free.dev/*
